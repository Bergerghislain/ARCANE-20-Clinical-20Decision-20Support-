[pytest]
testpaths = tests
addopts = -q --cov=backend_fastapi.app --cov-report=term-missing
markers =
  integration: tests nécessitant une base de données / services externes
  perf: tests de performance (fumee / seuils larges)

